package main;
import {
    "IO",
    "Math"
}

class Player {
    var <get> float64 health {
        get() {
            return this.health;
        }
    }
}

func int main() {
    var Player player;
    player.health = player.get<health()>;
    return 0;
}