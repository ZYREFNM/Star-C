package main;
import {
    "IO",
    "Math"
}

class Player {
    var <set, get> string name;
}

func int main() {
    var Player player;
    player.set<name("Dark")>;
    IO::Output.printfln("%s", player.get<name()>);
    return 0;
}