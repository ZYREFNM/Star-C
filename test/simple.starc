class Printer {
	var string text;
	func void Print() {
    	print this.text;
    }
}

func int main() {
	var Printer printer;
    printer.text = "quelque chose";
    Print(printer.text);
    printer.Print();
	return 0;
}